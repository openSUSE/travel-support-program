%li
  - if comment.private
    %span.private_label= t(:private_comment_label)
  = t(:comment_line, :date => l(comment.created_at, :format => :short), :user => comment.user.nickname, :body => comment.body)
