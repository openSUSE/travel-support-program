<p>
<%= t(:new_comment_title_line,
          :date => l(@comment.created_at, :format => :long),
          :machine => @machine.title,
          :user => @comment.user.nickname) %>
</p>

<% if @comment.private %>
  <p><b><%= t(:private_comment_label) %></b> <%= @machine.class.private_comment_hint %></p>
<% end %>

<p><%= @comment.body %></p>

<p><%= t(:visit_machine) %><br/><%= link_to(*[machine_url(@machine)]*2) %></p>

<p>--<br>
<%= Rails.configuration.site['email_footer'] %></p>
