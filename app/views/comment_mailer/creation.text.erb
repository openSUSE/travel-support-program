<%= t(:new_comment_title_line,
          :date => l(@comment.created_at, :format => :long),
          :machine => @machine.title,
          :user => @comment.user.nickname) %>
<% if @comment.private %>

<%= t(:private_comment_label) %> <%= @machine.class.private_comment_hint %>
<% end %>

<%= @comment.body %>

<%= t(:visit_machine) %>
<%= machine_url(@machine) %>

--
<%= TravelSupport::Config.setting :email_footer %>
