<%= t(:machine_in_state,
            :machine => @machine.title,
            :state => @state_name,
            :description => @machine.human_state_description,
            :date => (l(@updated_at) rescue "...")) %>

<%= @machine.human_state_guide %>

<% if @change %>
<%= t(:state_change_title_line, :date => l(@change.created_at, :format => :long), :event => @change.human_action_name, :user => @change.user.nickname) %> <%= t(:with_notes_below) %>

<%= @change.notes %>
<% end %>

<%= t(:visit_machine) %>

<%= machine_url(@machine) %>

--
<%= Rails.configuration.site['email_footer'] %>
